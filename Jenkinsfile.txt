pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package -B'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test -B'
            }
        }
    }
}